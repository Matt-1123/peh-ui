export type Cleanup = {
  id: string;
  title: string;
  description: string;
  createdAt: string;
  user: string;
  location: string;
  group_size: number;
  env_type: string;
  total_items: number;
  total_bags: number;
};